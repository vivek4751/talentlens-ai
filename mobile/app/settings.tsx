import { Pressable, StyleSheet, Text, View } from "react-native";
import { router } from "expo-router";
import { configuredEndpoint } from "@/lib/api";
import { colors } from "@/components/talentlens-ui";
export default function SettingsScreen() { return <View style={styles.screen}><Text style={styles.index}>MOBILE SETTINGS</Text><Text style={styles.title}>Connection, made visible.</Text><View style={styles.card}><Text style={styles.label}>BACKEND ADDRESS</Text><Text style={styles.value}>{configuredEndpoint() || "Not configured"}</Text><Text style={styles.copy}>Set EXPO_PUBLIC_TALENTLENS_API_URL to the deployed URL of your preserved Next.js TalentLens backend, then rebuild or restart Expo.</Text></View><Pressable onPress={() => router.back()} style={styles.close}><Text style={styles.closeText}>Close</Text></Pressable></View>; }
const styles = StyleSheet.create({ screen: { flex: 1, backgroundColor: colors.paper, padding: 22, paddingTop: 54 }, index: { color: colors.red, fontFamily: "SpaceMono", fontSize: 9, letterSpacing: 1 }, title: { color: colors.ink, fontSize: 34, fontWeight: "900", lineHeight: 34, letterSpacing: -2, marginTop: 8 }, card: { marginTop: 28, borderWidth: 1, borderColor: colors.line, backgroundColor: colors.soft, padding: 17 }, label: { color: colors.muted, fontFamily: "SpaceMono", fontSize: 9, letterSpacing: 0.9 }, value: { color: colors.ink, fontWeight: "800", fontSize: 13, marginTop: 8 }, copy: { color: colors.muted, fontSize: 11, lineHeight: 17, marginTop: 12 }, close: { alignItems: "center", padding: 18 }, closeText: { color: colors.red, fontFamily: "SpaceMono", fontSize: 10 } });
